let arr = [4, 2, 5, 19, 13, 0, 10].map(function(value){      
    return Math.pow (value,3);
});
console.log(arr);
let x = arr.reduce((sum, current) => sum + current, 0);
for (var i = 0; i < arr.length; i++) {
result = Math.sqrt(x);
}
console.log(result);